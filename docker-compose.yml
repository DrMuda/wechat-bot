version: '3'
services:
  wechat-bot:
    build: .
    container_name: wechat-bot
    ports:
      - "3000:3000"
    networks:
      - my-network
    volumes:
      - .:/app
    command: pnpm start

networks:
  my-network:
    driver: bridge
